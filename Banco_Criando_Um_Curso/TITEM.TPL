-- Criando a tabela TITEM
CREATE TABLE TITEM
 ( COD_ITEM INTEGER NOT NULL PRIMARY KEY,
   COD_CURSO INTEGER,
   COD_CONTRATO INTEGER,
   VALOR NUMBER(8,2) );
   
 INSERT INTO TITEM VALUES (1, 1, 1, 500);
 INSERT INTO TITEM VALUES (2, 1, 2, 500);
 INSERT INTO TITEM VALUES (3, 3, 3, 1500);
 INSERT INTO TITEM VALUES (4, 4, 4, 1600);
 INSERT INTO TITEM VALUES (5, 1, 5, 500);
 INSERT INTO TITEM VALUES (6, 1, 6, 500);
 INSERT INTO TITEM VALUES (7, 2, 6, 500);
 INSERT INTO TITEM VALUES (8, 3, 7, 500);
 COMMIT;

SELECT * FROM TITEM;

-- Realizando um update
UPDATE TCONTRATO TCO
SET TCO.DESCONTO = 18
WHERE TCO.COD_CONTRATO = 2;

UPDATE TCONTRATO TCO SET 
TCO.DESCONTO = 20,
TCO.DATA = '05/06/2023',
TCO.TOTAL = 2500
WHERE TCO.COD_CONTRATO = 2;

SELECT * FROM TCONTRATO;


UPDATE TALUNO TAL SET
TAL.CIDADE = 'MARANHÃO'
WHERE TAL.COD_ALUNO = 5;

SELECT * FROM TALUNO;

COMMIT;